spring:
  profiles:
    active: '@activatedProperties@'
  application:
    name: externalws-service

spring doc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /api/v1/api-docs

resilience4j:
  circuitbreaker:
    instances:
      soapCalculatorService: # <-- Nueva instancia para el cliente SOAP
        register-health-indicator: true
        sliding-window-type: COUNT_BASED
        sliding-window-size: 6           # Evaluamos sobre las últimas 6 llamadas
        minimum-number-of-calls: 3        # Mínimo de llamadas antes de calcular fallos
        failure-rate-threshold: 50        # Si el 50% falla, se abre el circuito
        wait-duration-in-open-state: 15s  # Espera 15 segundos antes de intentar Half-Open
        permitted-number-of-calls-in-half-open-state: 2
        automatic-transition-from-open-to-half-open-enabled: true
      soapcountryService: # <-- Nueva instancia para el cliente SOAP
        register-health-indicator: true
        sliding-window-type: COUNT_BASED
        sliding-window-size: 6           # Evaluamos sobre las últimas 6 llamadas
        minimum-number-of-calls: 3        # Mínimo de llamadas antes de calcular fallos
        failure-rate-threshold: 50        # Si el 50% falla, se abre el circuito
        wait-duration-in-open-state: 15s  # Espera 15 segundos antes de intentar Half-Open
        permitted-number-of-calls-in-half-open-state: 2
        automatic-transition-from-open-to-half-open-enabled: true        
  timelimiter:
    instances:
      soapCalculatorService:
        timeout-duration: 3s
        cancel-running-future: true
      soapcountryService:
        timeout-duration: 3s
        cancel-running-future: true        
  retry:
    instances:
      soapCalculatorService: # <-- Reintentos específicos para el SOAP
        max-attempts: 3
        wait-duration: 250ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
      soapcountryService: # <-- Reintentos específicos para el SOAP
        max-attempts: 3
        wait-duration: 250ms
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2        

management:
  endpoints:
    web:
      exposure:
        include: health,circuitbreakers,metrics
  endpoint:
    health:
      show-details: always
  health:
    circuit breakers:
      enabled: true

logging:
  # file:
  #   name: logs/app.log
  # logback:
  #   rolling policy:
  #     max-file-size: 20MB
  #     max-history: 15
  #     total-size-cap: 500MB
  #     file-name-pattern: logs/app-%d{yyyy-MM-dd}.%i.log.gz
  level:
    web: INFO
    hogar.codelive.externalws: INFO
    io.github.resilience4j: INFO
    com.zaxxer.hikari: WARN
    org.hibernate: WARN
    org.hibernate.SQL: WARN
    org.hibernate.orm.connections.pooling: WARN
    org.hibernate.type.descriptor.sql.BasicBinder: WARN    
    org.springframework.boot.autoconfigure.security.UserDetailsServiceAutoConfiguration: WARN
    org.hibernate.engine.transaction.jta.platform.internal.JtaPlatformInitiator: WARN
    org.springframework.boot.devtools.restart.ChangeableUrls: ERROR

queue:
  capacity: 10

pool:
  size: 5

cache:
  initialCapacity: 100
  maximumSize: 1000
  expireAfterWrite: 10 

soap:
  calculator:
    wsdl: ${http://www.dneonline.com/calculator.asmx}
  countryinfo:
    wsdl: ${http://webservices.oorsprong.org/websamples.countryinfo/CountryInfoService.wso}

api:
  typicode:
    url: ${https://jsonplaceholder.typicode.com}
